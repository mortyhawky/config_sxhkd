# Super + Enter → launch terminal
super + Return
    alacritty -e tmux

# Super + P → launch dmenu
super + p
    dmenu_run

# Super + W → launch BROWSER
super + w
    setsid $BROWSER >/dev/null 2>&1

# Ctrl + Super + R → reload sxhkd
ctrl + super + r
    pkill -USR1 -x sxhkd

